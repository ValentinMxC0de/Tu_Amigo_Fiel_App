<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.TuAmigoFiel">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.CRUD_SQLite"
        tools:targetApi="31">
        <activity
            android:name="com.example.TuAmigoFiel.PaginaPrincipal"
            android:exported="true"
            android:label="@string/title_activity_pagina_principal"
            android:theme="@style/Theme.CRUD_SQLite.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.TuAmigoFiel.MainActivity"
            android:exported="true"
            android:label="Ejemplo SQLite">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.TuAmigoFiel.Leer"
            android:label="Leer SQLite" />
        <activity
            android:name="com.example.TuAmigoFiel.Editar"
            android:label="Actualizar SQLite" />
    </application>

</manifest>